# ONBOARD

## Context 管理

- Context 管理本质上需要评估将一个旧上下文驱逐带来的未来可能收益和造成的前缀缓存失效代价之间的权衡。

### 文件阅读/编辑上下文

- 让 agent 维护一个编辑器的概念，它自行决定维护一组打开的文件，这些文件在上下文的某一处是出现过的；
- agent 自行选择文件的关闭管理，关闭后上下文将会被从上下文中驱逐掉；
- 编辑文本总是会将旧版上下文驱逐（如果缓存代价可以接受），并将新版本的上下文加入到上下文中；

### 工具调用结果

### 代码仓库上下文

- 基于 LSP 分析提供实时代码图
