diff --git a/dist/OpenAiLanguageModel.js b/dist/OpenAiLanguageModel.js
index 13bb809f394c357387084b96bcdff24f7fb260dc..22f849a2d54e70f62eb5d4a65d8306ea35958812 100644
--- a/dist/OpenAiLanguageModel.js
+++ b/dist/OpenAiLanguageModel.js
@@ -17,6 +17,7 @@ import * as Layer from "effect/Layer";
 import * as Option from "effect/Option";
 import * as Predicate from "effect/Predicate";
 import * as Redactable from "effect/Redactable";
+import * as Schema from "effect/Schema";
 import * as AST from "effect/SchemaAST";
 import * as Stream from "effect/Stream";
 import * as AiError from "effect/unstable/ai/AiError";
@@ -169,6 +170,7 @@ export const make = /*#__PURE__*/Effect.fnUntraced(function* ({
       const [rawResponse, response] = yield* client.createResponse(request);
       annotateResponse(options.span, rawResponse);
       return yield* makeResponse({
+        options,
         rawResponse,
         response,
         toolNameMapper
@@ -185,6 +187,7 @@ export const make = /*#__PURE__*/Effect.fnUntraced(function* ({
       annotateRequest(options.span, request);
       const [response, stream] = yield* client.createResponseStream(request);
       return yield* makeStreamResponse({
+        options,
         stream,
         response,
         toolNameMapper
@@ -524,6 +527,7 @@ const buildHttpResponseDetails = response => ({
   headers: Redactable.redact(response.headers)
 });
 const makeResponse = /*#__PURE__*/Effect.fnUntraced(function* ({
+  options,
   rawResponse,
   response,
   toolNameMapper
@@ -559,7 +563,7 @@ const makeResponse = /*#__PURE__*/Effect.fnUntraced(function* ({
         const toolId = toolCall.id ?? `${rawResponse.id}_tool_${index}`;
         const toolName = toolNameMapper.getCustomName(toolCall.function?.name ?? "unknown_tool");
         const toolParams = toolCall.function?.arguments ?? "{}";
-        const params = yield* Effect.try({
+        const encodedParams = yield* Effect.try({
           try: () => Tool.unsafeSecureJsonParse(toolParams),
           catch: cause => AiError.make({
             module: "OpenAiLanguageModel",
@@ -571,6 +575,7 @@ const makeResponse = /*#__PURE__*/Effect.fnUntraced(function* ({
             })
           })
         });
+        const params = yield* transformToolCallParams(options.tools, toolName, encodedParams);
         hasToolCalls = true;
         parts.push({
           type: "tool-call",
@@ -604,6 +609,7 @@ const makeResponse = /*#__PURE__*/Effect.fnUntraced(function* ({
   return parts;
 });
 const makeStreamResponse = /*#__PURE__*/Effect.fnUntraced(function* ({
+  options,
   stream,
   response,
   toolNameMapper
@@ -645,7 +651,7 @@ const makeStreamResponse = /*#__PURE__*/Effect.fnUntraced(function* ({
       }
       for (const toolCall of Object.values(activeToolCalls)) {
         const toolParams = toolCall.arguments.length > 0 ? toolCall.arguments : "{}";
-        const params = yield* Effect.try({
+        const encodedParams = yield* Effect.try({
           try: () => Tool.unsafeSecureJsonParse(toolParams),
           catch: cause => AiError.make({
             module: "OpenAiLanguageModel",
@@ -657,6 +663,7 @@ const makeStreamResponse = /*#__PURE__*/Effect.fnUntraced(function* ({
             })
           })
         });
+        const params = yield* transformToolCallParams(options.tools, toolCall.name, encodedParams);
         parts.push({
           type: "tool-params-end",
           id: toolCall.id
@@ -878,6 +885,35 @@ const unsupportedSchemaError = (error, method) => AiError.make({
     description: error instanceof Error ? error.message : String(error)
   })
 });
+const tryCodecTransform = (schema, method) => Effect.try({
+  try: () => toCodecOpenAI(schema),
+  catch: error => unsupportedSchemaError(error, method)
+});
+const transformToolCallParams = /*#__PURE__*/Effect.fnUntraced(function* (tools, toolName, toolParams) {
+  const tool = tools.find(tool => tool.name === toolName);
+  if (Predicate.isUndefined(tool)) {
+    return yield* AiError.make({
+      module: "OpenAiLanguageModel",
+      method: "makeResponse",
+      reason: new AiError.ToolNotFoundError({
+        toolName,
+        availableTools: tools.map(tool => tool.name)
+      })
+    });
+  }
+  const {
+    codec
+  } = yield* tryCodecTransform(tool.parametersSchema, "makeResponse");
+  return yield* Schema.decodeUnknownEffect(codec)(toolParams).pipe(Effect.mapError(error => AiError.make({
+    module: "OpenAiLanguageModel",
+    method: "makeResponse",
+    reason: new AiError.ToolParameterValidationError({
+      toolName,
+      toolParams,
+      description: error.issue.toString()
+    })
+  })));
+});
 const tryJsonSchema = (schema, method) => Effect.try({
   try: () => Tool.getJsonSchemaFromSchema(schema, {
     transformer: toCodecOpenAI
@@ -1118,10 +1154,28 @@ const toChatMessages = input => {
       content: input
     }];
   }
-  const messages = [];
-  for (const item of input) {
-    messages.push(...toChatMessagesFromItem(item));
-  }
+    const messages = [];
+    let toolCalls = [];
+    const flushToolCalls = () => {
+        if (toolCalls.length > 0) {
+            messages.push({ role: "assistant", content: null, tool_calls: toolCalls });
+            toolCalls = [];
+        }
+    };
+    for (const item of input) {
+        if (item.type === "function_call") {
+            toolCalls.push({
+                id: item.call_id,
+                type: "function",
+                function: { name: item.name, arguments: item.arguments }
+            });
+        }
+        else {
+            flushToolCalls();
+            messages.push(...toChatMessagesFromItem(item));
+        }
+    }
+    flushToolCalls();
   return messages;
 };
 const toChatMessagesFromItem = item => {
diff --git a/src/OpenAiLanguageModel.ts b/src/OpenAiLanguageModel.ts
index f4fa699196325a19ab6464968f3333cbbd987bca..839bcf6af3382c9478beab463221224cf2eaf357 100644
--- a/src/OpenAiLanguageModel.ts
+++ b/src/OpenAiLanguageModel.ts
@@ -17,7 +17,7 @@ import * as Layer from "effect/Layer"
 import * as Option from "effect/Option"
 import * as Predicate from "effect/Predicate"
 import * as Redactable from "effect/Redactable"
-import type * as Schema from "effect/Schema"
+import * as Schema from "effect/Schema"
 import * as AST from "effect/SchemaAST"
 import * as Stream from "effect/Stream"
 import type { Span } from "effect/Tracer"
@@ -628,6 +628,7 @@ export const make = Effect.fnUntraced(function*({ model, config: providerConfig
         const [rawResponse, response] = yield* client.createResponse(request)
         annotateResponse(options.span, rawResponse)
         return yield* makeResponse({
+          options,
           rawResponse,
           response,
           toolNameMapper
@@ -642,6 +643,7 @@ export const make = Effect.fnUntraced(function*({ model, config: providerConfig
         annotateRequest(options.span, request)
         const [response, stream] = yield* client.createResponseStream(request)
         return yield* makeStreamResponse({
+          options,
           stream,
           response,
           toolNameMapper
@@ -1104,10 +1106,12 @@ type ActiveToolCall = {
 
 const makeResponse = Effect.fnUntraced(
   function*<Tools extends ReadonlyArray<Tool.Any>>({
+    options,
     rawResponse,
     response,
     toolNameMapper
   }: {
+    readonly options: LanguageModel.ProviderOptions
     readonly rawResponse: CreateResponse200
     readonly response: HttpClientResponse.HttpClientResponse
     readonly toolNameMapper: Tool.NameMapper<Tools>
@@ -1147,7 +1151,7 @@ const makeResponse = Effect.fnUntraced(
           const toolId = toolCall.id ?? `${rawResponse.id}_tool_${index}`
           const toolName = toolNameMapper.getCustomName(toolCall.function?.name ?? "unknown_tool")
           const toolParams = toolCall.function?.arguments ?? "{}"
-          const params = yield* Effect.try({
+          const encodedParams = yield* Effect.try({
             try: () => Tool.unsafeSecureJsonParse(toolParams),
             catch: (cause) =>
               AiError.make({
@@ -1160,6 +1164,7 @@ const makeResponse = Effect.fnUntraced(
                 })
               })
           })
+          const params = yield* transformToolCallParams(options.tools, toolName, encodedParams)
           hasToolCalls = true
           parts.push({
             type: "tool-call",
@@ -1192,10 +1197,12 @@ const makeResponse = Effect.fnUntraced(
 
 const makeStreamResponse = Effect.fnUntraced(
   function*<Tools extends ReadonlyArray<Tool.Any>>({
+    options,
     stream,
     response,
     toolNameMapper
   }: {
+    readonly options: LanguageModel.ProviderOptions
     readonly stream: Stream.Stream<ResponseStreamEvent, AiError.AiError>
     readonly response: HttpClientResponse.HttpClientResponse
     readonly toolNameMapper: Tool.NameMapper<Tools>
@@ -1237,7 +1244,7 @@ const makeStreamResponse = Effect.fnUntraced(
 
           for (const toolCall of Object.values(activeToolCalls)) {
             const toolParams = toolCall.arguments.length > 0 ? toolCall.arguments : "{}"
-            const params = yield* Effect.try({
+            const encodedParams = yield* Effect.try({
               try: () => Tool.unsafeSecureJsonParse(toolParams),
               catch: (cause) =>
                 AiError.make({
@@ -1250,6 +1257,7 @@ const makeStreamResponse = Effect.fnUntraced(
                   })
                 })
             })
+            const params = yield* transformToolCallParams(options.tools, toolCall.name, encodedParams)
             parts.push({ type: "tool-params-end", id: toolCall.id })
             parts.push({
               type: "tool-call",
@@ -1461,6 +1469,46 @@ const unsupportedSchemaError = (error: unknown, method: string): AiError.AiError
     })
   })
 
+const tryCodecTransform = <S extends Schema.Constraint>(schema: S, method: string) =>
+  Effect.try({
+    try: () => toCodecOpenAI(schema),
+    catch: (error) => unsupportedSchemaError(error, method)
+  })
+
+const transformToolCallParams = Effect.fnUntraced(function*(
+  tools: ReadonlyArray<Tool.Any>,
+  toolName: string,
+  toolParams: unknown
+): Effect.fn.Return<unknown, AiError.AiError> {
+  const tool = tools.find((tool) => tool.name === toolName)
+
+  if (Predicate.isUndefined(tool)) {
+    return yield* AiError.make({
+      module: "OpenAiLanguageModel",
+      method: "makeResponse",
+      reason: new AiError.ToolNotFoundError({
+        toolName,
+        availableTools: tools.map((tool) => tool.name)
+      })
+    })
+  }
+
+  const { codec } = yield* tryCodecTransform(tool.parametersSchema, "makeResponse")
+  return yield* Schema.decodeUnknownEffect(codec)(toolParams).pipe(
+    Effect.mapError((error) =>
+      AiError.make({
+        module: "OpenAiLanguageModel",
+        method: "makeResponse",
+        reason: new AiError.ToolParameterValidationError({
+          toolName,
+          toolParams,
+          description: error.issue.toString()
+        })
+      })
+    )
+  )
+})
+
 const tryJsonSchema = <S extends Schema.Constraint>(schema: S, method: string) =>
   Effect.try({
     try: () => Tool.getJsonSchemaFromSchema(schema, { transformer: toCodecOpenAI }),
@@ -1721,10 +1769,32 @@ const toChatMessages = (
   }
 
   const messages: Array<CreateResponseRequestJson["messages"][number]> = []
+  let toolCalls: Array<{
+    readonly id: string
+    readonly type: "function"
+    readonly function: { readonly name: string; readonly arguments: string }
+  }> = []
+
+  const flushToolCalls = () => {
+    if (toolCalls.length > 0) {
+      messages.push({ role: "assistant", content: null, tool_calls: toolCalls })
+      toolCalls = []
+    }
+  }
 
   for (const item of input) {
-    messages.push(...toChatMessagesFromItem(item))
+    if (item.type === "function_call") {
+      toolCalls.push({
+        id: item.call_id,
+        type: "function",
+        function: { name: item.name, arguments: item.arguments }
+      })
+    } else {
+      flushToolCalls()
+      messages.push(...toChatMessagesFromItem(item))
+    }
   }
+  flushToolCalls()
 
   return messages
 }
